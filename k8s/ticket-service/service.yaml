apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: ticket-service
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3000