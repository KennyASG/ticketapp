apiVersion: v1
kind: Service
metadata:
  name: concert-service
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: concert-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000