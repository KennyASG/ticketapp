apiVersion: v1
kind: Service
metadata:
  name: venue-service
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: venue-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 30002